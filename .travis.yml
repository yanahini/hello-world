matrix:
  include:
    - language: python
      python: 
        - "3.6"
      before_script:
        - echo "Hola"
      script:
        - pytest
    - language: node_js
      node_js: 
        - "8.2.1"
      install:
        - npm install newman
      before_script:
          - node --version
          - npm --version
          - node_modules/.bin/newman --version
      script:
        - node_modules/.bin/newman run testpostman.postman_collection.json
        
     
